// Removed — the hero redesign replaced the globe with an animated product
// showcase (components/sections/hero-showcase.tsx). Kept as an empty module
// only so nothing breaks if an old import lingers; safe to delete.
export {}
